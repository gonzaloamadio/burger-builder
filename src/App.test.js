// import React from 'react';
// import ReactDOM from 'react-dom';
// import App from './App';


// We leave this commented out, because it gives an error.
// Could not find how to solve it, or either if this test is ok.
// It is difficult to "unit test" App, as now it involving
// routing, redux, etc.

// it('renders without crashing', () => {
//   const div = document.createElement('div');
//   ReactDOM.render(<App />, div);
//   ReactDOM.unmountComponentAtNode(div);
// });
